var darkmode = false;

function clickDarkMode() {
    if (darkmode) { // if darkmode is on
        console.log("Darkmode is on");
    } else { // if darkmode is off
        console.log("Darkmode is off");
    }
}